import { AuthUrl } from "@src/Auth/AuthUrl";

describe("Autenticação pela url", () => {
  it("Verifica se a url esta em conformidade com o esperado", () => {
    //
  });

  it("Realiza a ação de autenticação dos valores passados na url", () => {
    //
  });
});
