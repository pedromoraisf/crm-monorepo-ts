describe("Interface de autenticação com contato na camada operacional do objeto", () => {
  it("Valida as credenciais recebidas e quebra o processo", () => {
    //
  });

  it("Valida e adiciona as credenciais necessárias do usuário no objeto", () => {
    //
  });
});
